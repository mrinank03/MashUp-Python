# Email Configuration (Required for sending mashups)
EMAIL_ADDRESS=your_email@gmail.com
EMAIL_PASSWORD=your_app_password_here

# Fallback Mode (Recommended: true)
# Uses default.mp3 when YouTube blocks downloads
FALLBACK_MODE=true

# ============================================================
# YouTube Bot Bypass Configuration (Optional but Recommended)
# ============================================================

# Option 1: YouTube po_token and visitor_data (MOST EFFECTIVE for 2026)
# Get these using the method below:
YT_PO_TOKEN=
YT_VISITOR_DATA=

# Option 2: YouTube Cookies File (Alternative method)
# Export cookies from your browser and provide the path
YT_COOKIES_FILE=

# ============================================================
# HOW TO GET po_token and visitor_data
# ============================================================
#
# Method 1: Using Browser DevTools (Easiest)
# ------------------------------------------
# 1. Open youtube.com in Chrome/Firefox (logged in)
# 2. Open DevTools (F12 or Right-click -> Inspect)
# 3. Go to Network tab
# 4. Click on any video
# 5. In Network tab, find a request to "player" or "next"
# 6. Look in Request Headers for these cookies:
#    - Find "Cookie:" header
#    - Look for: VISITOR_INFO1_LIVE=<visitor_data>
#    - Look for: __Secure-YT-POTOKEN=<po_token>
# 7. Copy the values (without the cookie names)
# 8. Paste above
#
# Method 2: Using yt-dlp (Advanced)
# ----------------------------------
# Run this command in terminal:
# yt-dlp --print "%(requested_formats)s" "https://youtube.com/watch?v=dQw4w9WgXcQ" -v 2>&1 | grep -E "po_token|visitor_data"
#
# Or use browser extension like "EditThisCookie" to export cookies
#
# ============================================================
# HOW TO EXPORT COOKIES FILE
# ============================================================
#
# Option A: Using Browser Extension (Recommended)
# ------------------------------------------------
# 1. Install "Get cookies.txt LOCALLY" extension for Chrome/Firefox
#    Chrome: https://chrome.google.com/webstore (search "get cookies.txt")
#    Firefox: https://addons.mozilla.org (search "cookies.txt")
# 2. Go to youtube.com (make sure you're logged in)
# 3. Click the extension icon
# 4. Click "Export" or "Download"
# 5. Save as "youtube_cookies.txt" in this project folder
# 6. Set: YT_COOKIES_FILE=youtube_cookies.txt
#
# Option B: Using yt-dlp command
# ------------------------------
# yt-dlp --cookies-from-browser chrome --cookies youtube_cookies.txt "https://youtube.com"
# Then set: YT_COOKIES_FILE=youtube_cookies.txt
#
# ============================================================
# IMPORTANT NOTES
# ============================================================
#
# - po_token and visitor_data EXPIRE after a few days/weeks
#   You'll need to refresh them periodically
#
# - Cookies also expire but usually last longer (weeks/months)
#
# - If you set both, the app will use BOTH for maximum success
#
# - Even with these, YouTube may still block some requests
#   That's why FALLBACK_MODE=true is important!
#
# - For Streamlit Cloud deployment:
#   Add these to your app's Secrets instead of .env file
#   (Settings -> Secrets in Streamlit Cloud dashboard)
